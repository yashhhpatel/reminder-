package com.remindly.app.domain.model

enum class AppThemeMode {
    AUTO, LIGHT, DARK
}

enum class RepeatType {
    NONE, DAILY, WEEKDAY, WEEKLY, MONTHLY, YEARLY, CUSTOM
}

enum class SoundMode {
    RING_ONCE, REPEAT_UNTIL_DISMISSED, SILENT
}
